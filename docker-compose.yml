version: '3.7'

services:

  sample:
    container_name: ddd_viewer
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
    ports:
      - '3010:3000'
    environment:
      - NODE_ENV=development